HELLO GROUP
Make sure you check requirements.txt so that everything will be
Usable.
To install dependencies run: pip install -r requirements.txt (or pip3)
After this to run the app run: streamlit run app/streamlit_app.py